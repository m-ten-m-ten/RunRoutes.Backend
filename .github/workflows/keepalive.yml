# .github/workflows/keepalive.yml
name: Keep Alive
on:
  schedule:
    - cron: '0 0 */5 * *'  # 5日ごと
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -f https://runroutes.mten.dev/